version: '3.8'
services:
  client:
    build:
      args:
        NODE_ENV: production
        DEBUG:
      dockerfile: Dockerfile
    ports:
      - 80:80
  server:
    build:
      dockerfile: Dockerfile
